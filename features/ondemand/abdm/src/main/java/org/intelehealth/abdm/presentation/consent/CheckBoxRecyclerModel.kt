package org.intelehealth.abdm.presentation.consent

/** Row model for the consent checklist. Ported verbatim from the legacy module. */
internal class CheckBoxRecyclerModel(
    var checkboxText: String,
    var isChecked: Boolean,
    var isCheckboxEnabled: Boolean,
)
