<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidMobileClient" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidMobileClient" parent="Base.Theme.AndroidMobileClient" />

    <style name="style_subtitle">
        <item name="android:textSize">@dimen/text_13</item>
        <item name="android:textColor">@color/subtitle_color</item>
    </style>

    <style name="type_mobile_no">
        <item name="android:textSize">@dimen/text_15</item>
        <item name="android:textColor">@color/subtitle_color</item>
    </style>

    <style name="style_error_message">
        <item name="android:textSize">@dimen/text_14</item>
        <item name="android:textColor">@color/error_red</item>
    </style>

</resources>