<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="title"
            type="Integer" />

        <variable
            name="submitLabel"
            type="Integer" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/clDialogContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/white_child_container_bg"
            android:maxWidth="@dimen/dialog_max_width"
            android:maxHeight="@dimen/dialog_max_height"
            android:paddingStart="@dimen/screen_container_padding"
            android:paddingTop="@dimen/screen_container_padding"
            android:paddingEnd="@dimen/screen_container_padding"
            android:paddingBottom="@dimen/screen_container_padding"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/tvDialogTitle"
                style="@style/Theme.EZazi.Text.Dialog.Title"
                stringRes="@{title}"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/screen_container_padding"
                android:visibility="@{title == 0 ? View.GONE : View.VISIBLE}"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Yes" />

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/clDataContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/tvDialogTitle">

                <include layout="@layout/dialog_confirmation_view" />
            </androidx.constraintlayout.widget.ConstraintLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>

        <LinearLayout
            android:id="@+id/llDialogActions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal"
            android:paddingHorizontal="@dimen/dialog_btn_horizontal_margin"
            android:paddingVertical="@dimen/dialog_btn_vertical_margin"
            android:weightSum="2"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@id/clDialogContainer"
            app:layout_constraintStart_toStartOf="@id/clDialogContainer"
            app:layout_constraintTop_toBottomOf="@id/clDialogContainer">


            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDismiss"
                style="@style/Theme.EZazi.Dialog.Button.Cancel"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:contentDescription="@string/cancel"
                android:text="@string/cancel"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/includeLoginInput" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSubmit"
                style="@style/Theme.EZazi.Dialog.Button.Next"
                stringRes="@{submitLabel}"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/dialog_btn_horizontal_margin"
                android:layout_weight="1"
                android:contentDescription="@string/submit"
                android:text="@string/submit"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/includeLoginInput" />

        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>