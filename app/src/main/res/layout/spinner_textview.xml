<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/llLocationNameSetupSpinner"
    android:contentDescription="Setup Spinner Location Name Linear Layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <View
        android:id="@+id/viewDividerSpinner"
        android:contentDescription="Setup Spinner Divider View"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/gray_1"
        android:layout_marginHorizontal="10dp"
        android:layout_marginVertical="2dp"/>

    <TextView
        android:id="@+id/tvLocationNameSetupSpinner"
        android:contentDescription="Setup Spinner Location Name TextView"
        android:singleLine="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="marquee"
        android:text="Remote"
        android:layout_margin="10dp"
        android:textSize="14sp"
        android:fontFamily="@font/lato_regular"
        android:textAlignment="inherit"
        />

</LinearLayout>

