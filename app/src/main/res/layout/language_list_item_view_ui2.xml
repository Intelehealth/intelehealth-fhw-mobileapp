<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/llChooseLanguageItem"
    android:contentDescription="Choose Language Item LinearLayout"
    android:layout_marginStart="@dimen/parent_layout_margin"
    android:paddingStart="@dimen/parent_layout_padding_start"
    android:layout_marginEnd="@dimen/parent_layout_margin"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <RadioButton
        android:id="@+id/rbChooseLanguageItem"
        android:contentDescription="Choose Language Item RadioButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/rb_select_lang_padding"
        android:text="@string/language"
        android:button="@drawable/ui2_ic_circle"
        android:lineHeight="@dimen/rb_select_lang_line_height"
        android:textColor="@color/textColorBlack"
        android:textSize="@dimen/rb_select_lang_text_size" />

</LinearLayout>