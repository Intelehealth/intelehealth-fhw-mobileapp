<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rlParentNewAppBar"
    android:contentDescription="New App Bar Parent RelativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <include
        android:id="@+id/toolbarNewAppBar"
        android:contentDescription="New App Bar Toolbar"
        layout="@layout/layout_new_custom_toolbar_ui2" />

    <FrameLayout
        android:id="@+id/flContainerNewAppBar"
        android:contentDescription="New App Bar Container FrameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/toolbarNewAppBar"
        android:layout_marginTop="@dimen/margin_12"
        android:paddingBottom="@dimen/home_framelayout_padding_bottom"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />


</RelativeLayout>